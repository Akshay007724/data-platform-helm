# =============================================================================
# Image tags — managed automatically by CI (platform-services workflow).
# Do NOT edit manually. To pin a specific version, trigger a workflow_dispatch
# or push a tag (v*.*.*) to the relevant service directory.
# =============================================================================

dataIngestion:
  image:
    repository: ghcr.io/akshay007724/data-platform/data-ingestion
    tag: "latest"
    pullPolicy: Always

dataProcessor:
  image:
    repository: ghcr.io/akshay007724/data-platform/data-processor
    tag: "latest"
    pullPolicy: Always
